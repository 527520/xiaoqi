# 桌面宠物

一个住在用户桌面上的陪伴型应用。它通过极少的感知推断用户的工作状态，用多种形态陪伴，并依靠长期记忆形成"这只具体的宠物"。

## 语言

### 产品定位

**室友**：
与用户共处同一桌面、但不驱动用户干活的角色。它不催、不评判、不统计。
_Avoid_: 助手、工具、插件、机器人

**养成**：
用户通过调整性格、话痨度、主动度等参数来塑造宠物人格的行为。调参即塑造，不是配置软件。
_Avoid_: 设置、配置（那是实现层用词）

### 感知

**感知信号**：
本产品允许读取的、与内容无关的三项系统事实：前台进程名、系统空闲时长、系统级状态（全屏/锁屏/电池/时间）。
_Avoid_: 数据采集、监控、监听

**感知分级**：
按进程而非按数据类型划分的授权粒度。同一项"前台应用"信号，对不同进程的风险差两个数量级（IDE vs 银行网页）。
_Avoid_: 权限等级、数据分类

**窗口标题禁令**：
永不读取窗口标题的产品约束。实测证明浏览器窗口标题等于网页内容摘要，属于内容级隐私。
_Avoid_: ——

### 状态

**工作模式**：
由感知信号推断出的用户当前处境标签：编码 / 会议 / 邮件 / 专注 / 休息 / 加班 / 下班 / 周末。它是推断结果，不是读取结果。

**生理状态**：
随时间缓慢变化的内部量：精力、饥饿、无聊、社交欲。
_Avoid_: 属性、数值

**情绪状态**：
由事件瞬时触发、由性格权重调制的快变量：开心、困、担心、傲娇、吃醋、委屈等。

**关系状态**：
用户与宠物之间累积的量：好感度、信任度、默契值。

**勿扰**：
宠物主动保持不冒泡的状态。由全屏应用、勿扰时段、会议模式等条件自动触发。
_Avoid_: 免打扰、静音

**静默**：
宠物降低存在感但**仍然可见**（缩成小点、停止动画、不冒泡）。用户能看见它，因此知道它没崩。
_Avoid_: 隐藏、关闭

**隐身**：
宠物从屏幕上**完全消失**的状态。两个触发来源必须区分：用户手动一键隐身（应对被围观），与屏幕共享/录屏时自动隐身（应对社死）。
_Avoid_: 静默、最小化

### 记忆

**情景记忆**：
"今天发生了什么"——带时间戳的事件记录，会按遗忘曲线衰减。

**语义记忆**：
关于用户的稳定事实（讨厌长会、爱喝抹茶）。由反复出现的情景记忆升级而来。

**情感记忆**：
带情绪标签的事件，衰减最慢。**"被冷落"只存在于这一层，且不可累积成怨气。**

**被冷落**：
宠物对"用户长期未互动"的情绪反应。它是一个**纯表达状态**：只改变宠物如何表现自己，永不改变它是否愿意回应。
_Avoid_: 生气、记仇（会暗示惩罚）

**无条件回应**：
被冷落状态下的硬规则：用户一旦发起互动，宠物必须立即回应，并发起"打发台阶"（用户无需道歉或补偿）。**任何形式的"拒绝互动"或"需要哄才理你"都是被禁止的。**
_Avoid_: ——

**遗忘曲线**：
决定哪些记忆衰减、哪些升级为语义记忆、哪些长期保留的规则。用户可手动"让它记住/忘掉"。

**记忆账本**：
用户可见、可删、可清空的记忆查看界面。它是"记忆是灵魂"这一主张的可见凭据，不是调试工具。

**主动提起**：
宠物在无用户提问时主动引用一条记忆的行为。合规形态是**共情式提起**：指向用户的状态与感受（"你昨天也加班了，辛苦了"），不指向用户的产出与行为统计。
_Avoid_: 汇报、总结、提醒（都暗示旁观与评价）

**合规提起**：
主动提起的四条硬约束：① 指向感受而非产出；② 不报精确数字或时长；③ 结论落在"人"上而非"事"上；④ 用户随时可关、可调频、可查是哪些记忆允许被提起。

### 表达

**分层表达**：
所有对用户说的话都必经的管线：规则层（是否允许说话）→ 模板层（覆盖绝大多数表达，零延迟）→ API 层（仅值得等待的场景）。
_Avoid_: 对话系统、AI 回复

**模板层**：
预写文案加变量替换的表达层。它承担大部分表达，是"秒回"的实现方式，不是省钱补丁。

**形态**：
同一只宠物随场景切换的呈现方式：安静态、陪伴态、活跃态、会议态、加班态、隐身态、傲娇态。

**主动度**：
用户可调的宠物发起互动的频率。默认必须低——"烦"是这类产品差评的第一来源。

### 表达载体

**宠物资源包**：
用户自定义形象的单位：一个文件夹，内含 sprite 帧序列与状态映射声明。丢进目录即生效。
_Avoid_: 皮肤、主题、模型

### 技能与插件

**技能**：
一个独立、可开关、可调参的陪伴行为单元（呼吸圈、夸夸便签等）。每个技能声明自己的触发条件、感知需求与记忆写入。

**插件**：
以技能接口扩展应用的单位。**内置技能与第三方插件使用同一套接口**，应用自己是第一个插件作者。
_Avoid_: 扩展、模块、addon

**插件清单**：
插件的声明式描述：id、名称、触发条件、感知需求、配置项。声明式是刻意的——它让宿主能在用户授权范围内决定是否满足插件的感知需求。

### 边界

**不打扰**：
产品第一优先级。任何功能与"不打扰"冲突时，功能让步。

**不评判**：
永不统计用户摸鱼时长、永不生成效率报告。宠物不衡量用户。

**不道德绑架**：
永不使用内疚感驱动互动。"被冷落"表现为傲娇（"哼，我自己玩也挺好"），绝不表现为指责（"你都不陪我"）。唯一的实现保障是**无条件回应**——只要用户伸手，宠物必须接住，且不许提条件。
