# 阻塞项

施工令 §6 明确：**遇到预期内的阻塞不要卡住，把状态写清楚然后继续开发。**

**当前无阻塞。**

---

## 已解除

### B-1 · GitHub 远端仓库未创建（2026-09-11 解除）

**状态**：✅ 已解除 —— 远端仓库存在且已成功推送。

施工令 §6 预判本机"无 `gh` CLI、无 token，可能无法建仓库"。实际推送时确认
**远端仓库已经存在**，因此该预判的阻塞并未发生：

```
$ git push -u origin main
To github.com:527520/xiaoqi.git
   8db19b7..8c26797  main -> main
branch 'main' set up to track 'origin/main'.

$ git ls-remote --heads origin
8c2679700b9d55c767a93a1c51b20496bae7aebb  refs/heads/main

$ git status --short --branch
## main...origin/main
```

本地与远端指向同一个提交，SSH 推送正常（未遇到施工令 §3 提到的
`Connection closed by 198.18.0.9 port 22` 抖动）。

**尚未做**：开 PR。施工令 §6 期望"每个里程碑完成后推分支 + 开 PR"，
但本机没有 `gh` CLI，无法用命令行开 PR，而 `main` 是直接推送的
（本阶段只有一个人一条线，squash merge 的意义有限）。
**若要启用 PR 流程，需要用户决定**：是补装 `gh`，还是提供 token，
还是维持"直接推 main"的现状直到有协作者为止。

---

## 非阻塞的已知限制（不是阻塞项，但要如实标注）

这些不是"等用户决定"，而是**本机硬件条件所限、暂时无法验证**的事项。
实现按规格写了，但**不声称通过**：

| 项                                   | 原因                                       | 处置                                                       |
| ------------------------------------ | ------------------------------------------ | ---------------------------------------------------------- |
| 多显示器定位                         | 本机只有一块显示器（2560×1440 @1x）        | 代码按 `getDisplayNearestPoint` + 工作区放置写；**未验证** |
| 混合 DPI / 文字缩放                  | 本机 1x、无文字缩放                        | 用 `resolution: 1` + 全 DIP 坐标从设计上规避；**未验证**   |
| 「全屏应用下宠物不变空白」的人眼确认 | 脚本无法制造真实全屏应用                   | 已留 `XIAOQI_KEEP_OPEN_MS` 供人工观察；**待人工确认**      |
| 全屏时宠物视觉上是否进入静默         | 同上，需人眼 + 真实全屏应用                | 同上；**待人工确认**                                       |
| 托盘菜单与全局快捷键的交互           | 本机没有可自动化的输入通道（点托盘、按键） | **待人工点一次**                                           |
| 捕获排除对第三方录制工具的覆盖度     | 只测了 `desktopCapturer`                   | 需用 OBS / 截图工具手工复测                                |

**已在本机实测通过、不再是待确认项**：一键隐身的捕获排除
（见 `docs/verify-m1.md` 证据 3 —— 保护开启后宠物在整屏捕获中**一个像素都不剩**）。

---

## 技术债（有意留下，不是遗漏）

| 项                                     | 说明                                                             | 计划                                                            |
| -------------------------------------- | ---------------------------------------------------------------- | --------------------------------------------------------------- |
| Pixi 容器路径不渲染的**根因**          | 只确认了规避方式（不套中间 Container），未定位是哪个共享状态导致 | 已写进 `PetStage.ts` 注释标注"未定位"；若后续再遇到相关现象再查 |
| `assets/pets/xiaoqi/` 尚未落地为资源包 | v0.1 默认宠物是程序化几何角色，不读图片                          | M6 之后的"形象可自定义"再引入                                   |
| 未配置 CI                              | 施工令 §6 期望 CI 绿灯，但本阶段先保证本地四道质量门             | M6（打包与文档）一并做                                          |
| 未配置 electron-builder 打包           | `electron-builder.yml` 尚未创建，`pnpm dist` 目前会失败          | M6                                                              |
| 渲染产物 1.4MB 主 chunk                | PixiJS 全量打包（含 WebGPU 后端，本机不用）                      | M6 打包时再看是否需要裁减；开发期不阻塞                         |
