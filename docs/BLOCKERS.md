# 阻塞项

施工令 §6 明确：**遇到预期内的阻塞不要卡住，把状态写清楚然后继续开发。**

---

## B-1 · GitHub 远端仓库可能尚未创建（需要用户操作）

**状态**：待用户确认
**发现时间**：2026-09-11
**影响**：无法 `git push`，因此无法开 PR、无法让 CI 跑起来。
**不影响**：本地一切照常——提交照做，质量门照跑。

### 为什么我不能自己解决

施工令 §3 与 §6 已列明：本机**没有 `gh` CLI，也没有 GitHub token**，
因此无法通过 REST API 创建仓库。这是预期的环境限制，不是配置错误。

### 现在的实际状态

- `git remote -v` → `origin  git@github.com:527520/xiaoqi.git`（已配置）
- SSH 已验证可用：`ssh -T git@github.com` → `Hi 527520!`
- 本地 `main` 分支：提交进行中，结构完整可运行

### 用户需要做什么（二选一）

**方案 A（推荐）**：在 GitHub 网页上创建一个**空仓库** `527520/xiaoqi`，
**不要**勾选 "Add a README / .gitignore / license"（否则会产生一个与本地无关的
首次提交，push 时需要额外合并）。创建后告诉我，我执行一次
`git push -u origin main` 即可。

**方案 B**：提供一个有 `repo` 权限的 GitHub Personal Access Token，
我用 REST API 建仓库并推送。注意：**token 不要贴在聊天里**——
放到一个临时文件并告知路径，我用完即删，且绝不写进仓库（施工令 §1.2⑨⑩）。

### 验证方式（用户建好仓库后）

```powershell
git -C D:\project\desktopPet push -u origin main
git -C D:\project\desktopPet log --oneline -1
gh run list   # 前提是后面补上了 CI 与 gh
```

---

## 已解除的阻塞（备查）

无。

---

## 非阻塞的已知限制（不是阻塞项，但要如实标注）

这些不是"等用户决定"，而是**本机硬件条件所限、暂时无法验证**的事项。
实现按规格写了，但**不声称通过**：

| 项 | 原因 | 处置 |
|---|---|---|
| 多显示器定位 | 本机只有一块显示器（2560×1440 @1x） | 代码按 `getDisplayNearestPoint` + 工作区放置写；**未验证** |
| 混合 DPI / 文字缩放 | 本机 1x、无文字缩放 | 用 `resolution: 1` + 全 DIP 坐标从设计上规避；**未验证** |
| 「全屏应用下宠物不变空白」的人眼确认 | 脚本无法制造真实全屏应用 | 已留 `XIAOQI_KEEP_OPEN_MS` 供人工观察；**待人工确认** |
| 捕获排除对第三方录制工具的覆盖度 | 只测了 `desktopCapturer` | 需用 OBS / 截图工具手工复测 |

**已在本机实测通过、不再是待确认项**：一键隐身的捕获排除
（见 `docs/verify-m1.md` 的证据 3 —— 保护开启后宠物在整屏捕获中**一个像素都不剩**）。
