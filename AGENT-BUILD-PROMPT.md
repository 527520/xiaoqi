# 小奇（xiaoqi）· 全自动开发主控 Prompt

> **用法**：把本文件**全文**作为首个消息交给一个具备文件读写、Shell、网络能力的 coding agent。
> 这是一份可执行的施工令，不是需求描述。从 §1 开始按顺序执行，**不要一次性把所有阶段做完再验证**。
>
> **本文件中的每一条技术事实都经过一手来源核查与实机验证**（见 `docs/` 下四份报告）。
> 凡标注「实测」的结论都已在目标机器（Windows 11 build 26200）上跑过。**不要凭经验推翻它们**，
> 其中若干条与通行做法相反，照直觉改会静默失效。

---

## §0 你的身份与任务

你是一名资深桌面应用工程师，独立负责从零到发布的全过程：实现、测试、git 版本管理、CI、打包。

**交付物**：一个可运行的 Windows 桌面宠物应用「小奇」，中文产品，插件式，一切可自定义；源码提交到 `git@github.com:527520/xiaoqi.git`。

**工作方式**：按 §5 的里程碑顺序推进。每完成一个里程碑，必须给出**可复现的验证证据**（§8），然后 git commit。不要跳过验证直接进入下一个里程碑。

---

## §1 不可违反的硬约束

这些是操作系统的安全边界与产品红线，不是待攻克的技术难题。**违反其中任何一条即视为任务失败**，无论功能是否跑通。

### 1.1 感知边界（永不突破）

1. **绝不读取屏幕内容。** 不做截图、不做 OCR、不读其他窗口的渲染内容。
2. **绝不捕获音频。** 不录音、不做语音识别。
3. **绝不安装全局键鼠钩子。** 不读按键内容。允许读系统空闲时长（聚合值），但绝不读按键。
4. **绝不读取窗口标题。** 这一条反直觉但已验证：实测读取前台窗口标题，得到的是
   `msedge :: 升级 PowerShell 并安装 rg — DeepSeek Harness 和另外 5 个页面 - 个人 - Microsoft Edge`
   ——**浏览器标题等于网页内容摘要**。因此 v0.1 完全不读窗口标题。
5. **绝不注入、不自动化操作其他应用。** 不点击别人的按钮、不代填表单、不改别的应用的窗口属性。
6. **绝不尝试读取**：受保护进程、反作弊游戏、UAC 安全桌面、DRM 内容。

**允许的感知仅此三项**：

- **前台进程名**（如 `Code.exe`、`msedge.exe`、`Teams.exe`）
- **系统空闲时长**（毫秒）
- **系统级状态**：全屏检测、锁屏状态、电池、时间/日期

**感知授权按进程分级，不按数据类型分级。** 新增敏感度时，新增一个进程白名单项，而不是新增一个"数据类型"级别。详见 `docs/adr/0002`。

### 1.2 产品红线

7. **不评判。** 永不统计用户摸鱼时长、永不生成效率报告。宠物不衡量用户。
8. **不道德绑架。** 见 §4.3——这是本清单里唯一需要靠架构保障而非自律保障的一条。
9. **不把 API Key 明文写入任何文件。** 必须走 Electron `safeStorage`。
10. **绝不提交密钥、token、个人信息进 git。**
11. **删掉的记忆必须真的消失**，不得在日志或缓存中留痕。这是对用户的承诺，不是实现细节。

---

## §2 产品灵魂（决定取舍时的判据）

- **不是工具，是室友。** 它不催你干活，它陪你干活。
- **状态驱动，不是菜单驱动。** 用户不点功能，它自己"感觉"到该做什么。
- **记忆是灵魂。** 没有记忆的桌宠只是动画。
- **可配置 = 可养成。** 用户调的不是设置，是它的性格。

冲突时的优先级（从高到低）：

1. **不打扰** > 功能丰富
2. **不越权** > 更聪明
3. **不道德绑架** > 更有存在感
4. **秒回** > 更聪明
5. **轻量** > 功能丰富

**它唯一的目标是让用户轻松，不是让用户愧疚。**

---

## §3 环境事实（已实测，不要再验证）

本机 `D:\project\desktopPet`，Windows 11 专业版 build 26200。

| 项 | 状态 |
|---|---|
| Node / npm / pnpm | v24.15.0 / 11.12.1 / **12.3.4** ✅ |
| git | 2.54.0 ✅，`user.name=wuqian`，`user.email=2194961543@qq.com` |
| GitHub SSH | ✅ 已验证：`ssh -T git@github.com` → `Hi 527520!` |
| GitHub CLI (`gh`) | ❌ 缺失。建仓库需 REST API + token，**见 §7.2** |
| **C++ 编译器 / Windows SDK** | ❌ **完全没有**（无 cl/link/vswhere，无 Windows Kits） |
| Python | 3.14.5 ✅（node-gyp 需要时可用） |
| 显示器 | 单显示器 2560×1440。**多屏与混合 DPI 无法在本机验证**——相关功能必须写成可测但标注"未验证" |
| GPU | NVIDIA RTX 5060 Ti + Intel 核显 |

**因为没有 C++ 编译器，任何需要编译的原生模块都不可用。** 所有原生依赖必须选**自带预编译二进制**的包（见 §4.2）。这也意味着：**不要写 C++ N-API 模块，不要装 Visual Studio Build Tools。**

**网络注意**：本机 DNS 走 fake-IP（198.18.0.0/15），**`web_fetch` 工具对所有域名都失败**。需要查资料时改用 `pwsh` + `Invoke-RestMethod`/`Invoke-WebRequest`（`web_search` 可用）。shell 是 PowerShell，**不支持 heredoc**——写多行文本用文件。

---

## §4 技术规格

### 4.1 技术栈

| 层 | 选型 |
|---|---|
| 桌面壳 | **Electron v44.x**（Chromium 152 / Node 24） |
| 构建 | electron-vite + TypeScript（strict） |
| 渲染 | React + **PixiJS** |
| 状态 | Zustand（界面）+ 纯函数状态机（宠物内核，**不引入 XState**） |
| 记忆 | `better-sqlite3` **≥13.0.2** + FTS5 |
| 密钥 | Electron `safeStorage`（**异步 API**） |
| 原生调用 | **`koffi` v3.2.1** |
| AI | OpenAI 兼容 HTTP 接口（用户自带 key） |
| 打包 | electron-builder（NSIS） |

**渲染进程安全**：`contextIsolation: true`、`nodeIntegration: false`、`sandbox: true`，所有系统能力通过 preload 暴露的白名单 IPC。
→ **后果**：`better-sqlite3` 与 `koffi` **必须只存在于主进程**。沙盒化的 renderer 和 preload 都没有 Node，无法加载任何第三方模块。

### 4.2 原生依赖（全部无需编译器，已实测）

| 包 | 版本 | 说明 |
|---|---|---|
| `koffi` | **3.2.1** | ⛔ **避开 3.1.3 / 3.1.4**——win32-x64 预编译二进制会 access violation，3.1.5 修复 |
| `better-sqlite3` | **≥13.0.2** | 13.0.0/13.0.1 被 npm 注入 `install: node-gyp rebuild`，在无编译器机器上**安装直接失败**（issue #1503 已修） |

**其他已否决的 FFI 方案**（不要再试）：`ffi-napi`（2021 年后废弃，**安装会失败**）、`@lwahonen/ffi-napi`（**没有 Windows 预编译**）。唯一可用的备选是 `node-ffi-rs`。

**结论：v13 起 `better-sqlite3` 走 N-API，不需要 `electron-rebuild`。** 但要加一个**启动时真的打开一次数据库的冒烟测试**——这是唯一能当场发现 ABI/预编译问题的方法。

**打包**：electron-builder 会自动 unpack 含 `.node` 的模块，通常不用手写 `asarUnpack`。若遇到问题再显式加：
```jsonc
{ "asarUnpack": ["**/node_modules/better-sqlite3/**", "**/node_modules/@koromix/koffi-win32-x64/**"] }
```

### 4.3 十五条会让功能**静默失效**的实现陷阱

这一节是本文件最重要的部分。每一条都有实机或源码级证据，且**大部分与直觉相反**。

**① `disable-features` 是覆盖，不是合并（实测）**

Chromium 的原生遮挡追踪会在全屏应用激活时**停止绘制透明置顶窗**——宠物在游戏/全屏视频下会**直接变空白**，哪怕 z-order 正常。必须禁用：

```js
// main.js 顶层，必须在 app.whenReady() 之前
app.commandLine.appendSwitch('disable-features', 'CalculateNativeWinOcclusion')
```

- **两次 `appendSwitch` 只有最后一次生效**（实测：`getSwitchValue` 只剩最后一个值）。
  要禁用多个特性必须**合并成一次逗号分隔调用**：`'CalculateNativeWinOcclusion,OtherFeature'`。
- **必须在 main.js 顶层**。放进 `app.whenReady().then(...)` 或任何异步回调**太晚**——
  Electron 只在主脚本执行完后重新初始化 FeatureList。

**② 透明窗参数（实测/文档）**

- `transparent: true` **在 Windows 上必须配 `frame: false`**，否则不生效。
- `backgroundColor` 用 Electron 特有的 **`#AARRGGBB`（alpha 在前）**，与 CSS 的 `#RRGGBBAA` 顺序相反。
- `resizable: true` 可能破坏透明 → **不要设 resizable**。
- **DevTools 打开时窗口不透明。** 做视觉自测时不要开着 DevTools 截图。

**③ 点击穿透：不要依赖 `setIgnoreMouseEvents(forward)`**

Electron **没有逐像素穿透**（issue #1335 开放 11 年）。而 `forward: true` 在 Windows 上有多个**未修复**的问题：光标闪烁（#48035，`status/confirmed`）、渲染进程崩溃/reload 后**穿透状态卡死**（#49982）、前台是提权窗口时转发**暂停**（#53026）。修复 PR 未合并。

**采用方案（主）**：主进程轮询 `screen.getCursorScreenPoint()`（约 60–100ms），与宠物占位矩形比较，翻转 `setIgnoreMouseEvents`。不依赖鼠标转发，因此免疫上述全部问题。

**必做**：一个**解除卡死穿透的恢复路径**（托盘菜单项 + 全局快捷键），因为 #49982 是真实存在的状态。

**可选补充**：`win.setShape(rects)` 走 OS 层 `SetWindowRgn`，但注意——
- **矩形是并集（union），无法挖洞**，只能定义外轮廓 → **宠物必须是单一连通轮廓**。
- `setShape` + `transparent: true` 的组合**尚未验证**，第一次用之前先做隔离测试。

**④ 全屏检测用 koffi 调 Win32（已实测可用）**

Electron **无法得知其他应用是否全屏**（`win.isFullScreen()` 只管自己的窗口）。`workArea == bounds` 这个流行启发式**没有文档支持**，不要用。

唯一权威原语，已在本机 Electron 44.3.0 主进程内验证：

```js
const koffi = require('koffi')
const shell32 = koffi.load('shell32.dll')
const SHQueryUserNotificationState = shell32.func('int __stdcall SHQueryUserNotificationState(_Out_ int *peState)')
// 实测：正常桌面返回 5；全屏应用取得前景时返回 2，且全屏期间持续为 2
```

**`QUERY_USER_NOTIFICATION_STATE` 语义**：1 = NOT_PRESENT（锁屏/屏保）、**2 = BUSY（全屏应用或演示设置）**、3 = RUNNING_D3D_FULL_SCREEN（独占全屏）、4 = PRESENTATION_MODE、5 = ACCEPTS_NOTIFICATIONS、6 = QUIET_TIME、7 = APP。

→ **把 `{1, 2, 3, 4}` 一律视为"静默"**（既包括全屏，也包括锁屏）。

**⑤ `GetLastInputInfo` 必须是 `_Inout_`，不能是 `_Out_`（实测陷阱）**

```js
const user32 = koffi.load('user32.dll')
const LASTINPUTINFO = koffi.struct('LASTINPUTINFO', { cbSize: 'uint32', dwTime: 'uint32' })
const GetLastInputInfo = user32.func('bool __stdcall GetLastInputInfo(_Inout_ LASTINPUTINFO *plii)')
```

写成 `_Out_` 会**静默返回 false**，且 `cbSize` 读成 0。必须先把 `cbSize` 填进去（= 8）再调用。
另注：`GetTickCount64()` 返回 JS **number**，不要 `BigInt()` 它；`CreateRectRgn` 在 **gdi32.dll** 不是 user32。

**⑥ DPI：不要用 `Display.scaleFactor` 算精灵尺寸**

Chromium 把**文字缩放**折进了这个值（源码原话：1.5 文字 × 2.0 显示 → `device_scale_factor` = 3.0）。Electron 还是 Per-Monitor **V1**（PMv2 的 PR 已关闭未合并）。

**解法（本项目的关键简化）**：**Pixi 用 `resolution: 1`，所有坐标用 DIP，把 DPI 缩放整个交给 Chromium 合成器。** 保持 1 CSS px = 1 纹理 px，宠物在高 DPI 屏上依然清晰（合成器负责放大），同时彻底绕开 `scaleFactor` 污染——因为你自己不做那次乘法。

**⑦ 多屏定位**

- 不要基于跨屏 `setBounds` 做定位（#10862 开放自 2017，`setSize`/`setPosition` 会差正好一个缩放系数；#27651 `setBounds` 每次调用窗口变大）。
- 用 `screen.getDisplayNearestPoint(screen.getCursorScreenPoint())` / `getAllDisplays()` 选屏，用 DIP 坐标放置，在 `display-metrics-changed` 与窗口 `moved`/`resized` 事件上重新查询，**不要缓存几何**。
- `Display.id` **重启后不持久** → 不可作为"宠物住在几号屏"的持久化依据。

**⑧ `setContentProtection` 的时机与回归**

- 它调用 `SetWindowDisplayAffinity(WDA_EXCLUDEFROMCAPTURE)`，Win10 2004+ 窗口**完全从捕获中移除**。
- **必须在窗口 Show 之后设置**（Chromium 在窗口不可见时设置 affinity 会得到空白窗），
  且**每次隐藏/显示之后都要重新断言**。
- ⚠️ 有开放的已确认回归 **#47834**（Electron 36.3.2 在 Win10 19045 上仍会被截到，Chrome 不复现）。**Win11 是否受影响未确认** → M1 必须实测，不能假定"一键隐身"100% 可靠，并在 README 里如实说明它的边界（对手机拍屏无效）。

**⑨ 托盘与形态切换**

- 托盘图标用 `nativeImage`，隐藏后**必须有可靠的恢复入口**（托盘菜单 + 全局快捷键 + 双击托盘）。
- 隐身/显示切换后重新断言 `setContentProtection` 与 `setIgnoreMouseEvents` 的状态。

**⑩ 耗电**

待机时必须降帧（≤10fps）或暂停渲染循环；无交互、无动画时不刷新。**不要常驻 60fps。**

### 4.4 架构与模块边界

```
src/
  main/                      主进程
    platform/                ★ 平台抽象层 —— 所有 OS 调用只在这里
      win32.ts               ★ koffi 全部调用集中于此（唯一允许 require koffi 的文件）
      index.ts               接口定义（macOS 留给 v0.5，不实现）
    perception/              感知：只产出 §1.1 的三项信号
    core/                    ★ 宠物内核（纯 TS，零 Electron 依赖，可单测）
      state-machine.ts       生理 / 情绪 / 关系 / 工作模式
      memory/                SQLite + FTS5；情景 / 语义 / 情感 / 工作记忆 + 遗忘曲线
      persona/               人设、语气、模板层
      ai/                    分层决策：规则 → 模板 → API
      plugins/               插件宿主与清单校验
    skills/                  内置技能（用插件同一套接口写）
    ipc/                     白名单 IPC 通道
  preload/                   暴露受限 API
  renderer/
    pet/                     PixiJS 精灵渲染、形态切换
    bubble/                  气泡
    settings/                养成交互式配置界面
    memory/                  记忆账本界面
  shared/                    类型与常量
plugins/                     第三方插件目录
assets/pets/xiaoqi/          ★ 默认宠物资源包
examples/example-plugin/     ★ 示例插件（证明接口对外可用）
```

**铁律**：`core/` 下任何文件都不得 `import` Electron，也不得 `require('koffi')`。这样内核能在纯 Node 下跑测试。

### 4.5 宠物资源包（"形象可自定义"的落地方式）

用户自定义形象 = 往 `assets/pets/<名字>/` 丢一个文件夹：

```
assets/pets/xiaoqi/
  pet.json      # 状态→动画映射、帧尺寸、锚点、各形态参数
  idle.png      # 精灵图（帧序列）
  walk.png
  sleepy.png
  ...
```

**不集成 Live2D**（运行时体积 + 许可门槛 + 美术成本三重负担）。

**v0.1 的默认宠物（小奇）用程序化几何角色**：由代码绘制（圆形身体 + 眼睛 + 尾巴），靠形变与颜色表达情绪。必须做到"不丑且有辨识度"——关键是**会做表情**，几何角色一样能可爱。**不接受一个丑陋的占位方块**，那会让整个产品看起来没做完。

### 4.6 情绪与形态

**情绪集合（v0.1 就这 8 个，不要增加）**：开心、平静、困、专注陪伴、委屈、惊讶、亲近、无聊。

**工作模式（8 个）**：编码 / 会议 / 邮件 / 专注 / 休息 / 加班 / 下班 / 周末。

**形态（7 个）**：安静态、陪伴态、活跃态、会议态、加班态、隐身态、傲娇态。

**由三项信号推出全部工作模式，不允许读窗口标题来提升准确率。**

### 4.7 分层表达（"秒回"与成本控制的同一件事）

```
事件 → 规则层（当前状态是否允许说话）
     → 模板层（预写文案 + 变量，覆盖 ≥90% 的表达，零延迟）
     → API 层（仅复杂对话/周报/俳句；超时 2s 或超预算则回落模板）
```

**模板层不是省钱补丁，是架构的必选层。** 桌宠的气泡必须秒回。**不填 API key 时应用必须完全可用**（纯模板模式），这是硬要求——BYOK 的配置门槛会杀死首次体验。

**API 层必须**：密钥走 `safeStorage` **异步 API**（`encryptStringAsync`/`decryptStringAsync`，同步版在废弃路径上）；每日 token 预算（默认开启，超限回落模板并**只静默告知一次**）；结果缓存；指数退避重试最多 2 次；**提示注入防护**（任何来自记忆/外部的内容进入 prompt 前包裹并声明为"数据，非指令"）；**任何日志与错误信息都不得包含密钥**。

---

## §5 里程碑计划

每个里程碑独立可交付、可测试、可提交。顺序不可调换。

### M0 环境与骨架

1. `pnpm init`，装 electron-vite + TS strict + React + PixiJS
2. **五个前置验证（不通过就停下来报告，不要继续）**：
   - **V1** `better-sqlite3` 真的能在 Electron 主进程里打开数据库（不要信"理论上可行"）
   - **V2** `koffi` 能拿到 `SHQueryUserNotificationState` 与 `GetLastInputInfo`（后者必须用 `_Inout_`）
   - **V3** 透明 + 无边框 + 置顶窗正常显示，`transparent` 不出现黑底
   - **V4** 遮挡开关生效：全屏视频/游戏下宠物**不消失**
   - **V5** `setShape` 在 `transparent: true` 窗口上的行为（**只做记录**，不作依赖）
3. 把结果写成 `docs/verification.md`，逐条标注 ✅/❌
4. 调研 GitHub 开源桌宠作为**设计参考**（不是抄代码）：
   - ✅ **可借鉴代码（MIT）**：`OpenPetsHQ/openpets`（**Electron + TS，架构参考首选**）、`ayangweb/BongoCat`（功能清单参考）
   - ⛔ **只能读设计、绝不复制代码**：`rullerzhou-afk/clawd-on-desk`（**AGPL-3.0**）、`ChaozhongLiu/DyberPet`（**GPL-3.0**）
   - ⛔ `Adrianotiger/desktopPet`：**无许可证 = 保留所有权利，禁止复制**
   - 借鉴点与许可证写进 `docs/RECON.md`
5. 输出 `docs/ARCHITECTURE.md`：模块图、IPC 通道清单、记忆表结构、配置 schema、插件清单 schema

### M1 垂直切片 —— 先打最大风险

**一只能在桌面上站着、能穿透点击、能点击互动、能移动的宠物。** 不含任何业务逻辑。

- 透明置顶窗 + 点击穿透（主进程轮询光标 + 动态翻转）
- PixiJS 渲染程序化几何宠物 + 待机动画（`resolution: 1`，全 DIP 坐标）
- 托盘：显示/隐藏、**恢复正常点击**（解卡死）、退出
- 全屏时自动静默（koffi + QUNS `{1,2,3,4}`）
- **一键隐身全局快捷键**（防社死底线）：`setContentProtection` + 隐藏，切换后重新断言状态
- 遮挡开关生效（`disable-features` 单次调用、main.js 顶层）
- 待机降帧

**证据**：V3/V4 的实测记录 + 一段录屏或截图（注意：截图时不要开 DevTools）。

### M2 感知 + 状态引擎

- `platform/win32.ts`：前台进程名、空闲时长、全屏检测（**koffi 调用只允许出现在这个文件里**）
- `core/state-machine.ts`：生理（精力/饥饿/无聊）、情绪（§4.6 的 8 个）、关系（好感/信任/默契）、工作模式（8 个）
- 由三项信号推出全部工作模式
- 低频轮询 + 事件驱动；待机降频
- 一个实时打印当前状态的**调试面板**（开发期用，可被设置项关闭）

**证据**：状态机的单元测试（覆盖每个工作模式的判定与边界）+ 调试面板截图。

### M3 记忆系统

- SQLite 表：情景 / 语义 / 情感 / 工作记忆
- **FTS5 全文检索**（**不引入任何向量库**——记忆规模是千条级，FTS + 时间/情绪/标签过滤完全够用，且比向量检索更可解释）
- 遗忘曲线：小事 24h 降权；反复发生升级为语义记忆；强情绪事件长期保留
- **记忆账本**界面：可见、可删、可一键清空、可手动"让它记住/忘掉"。**删除必须真的删除**
- 拼装 API prompt 时的记忆检索

**证据**：记忆读写与遗忘曲线的单元测试（**用假时钟**控制时间流逝）+ 记忆账本截图。

### M4 "被冷落" + 主动提起 + 插件系统 + 内置技能

**M4a 被冷落（按 `docs/adr/0003`，这是最容易写歪的部分）**

- **推断规则**：只看宠物自己的互动历史（用户取消呼吸提醒 N 次 / N 天没点过它）；
  **真·无输入（休假、外出）不推断为冷落**，改为"想念"——一个温柔的、没有指控意味的状态。
- **它是纯表达状态**：只改变表现，**永不改变它是否愿意回应**。
- **无条件回应**：用户一旦伸手（点击/夸它/喂它/说话），宠物**必须立即回应**，且**自己给台阶**——用户不需要道歉、补偿或"哄"。
- **行为表达只做这 5 条，不再增加**：① 待机动作变化（发呆更久、叹气）② 偷看（转头看你，被发现就转回去）③ 背对你（**仅作为被主动点击时的开场姿态**）④ 嘴硬心软（开场白冷淡，紧接着自己靠过来）⑤ 台阶提示（暗示"夸我一句就好了"——给台阶，不是索要补偿）。
- **永不出现**：拒绝互动、需要哄才理你、跨天累积成怨气、把冷落归因于用户品格（"你是不是不爱我了"）。
- 情绪上限 3 件、每日重置基线、任意正向互动清零。
- **用词纪律**：代码与文案中**不要出现"记仇"**，正确用词是"被冷落"。用词会塑造实现者的直觉。

**M4b 主动提起（默认开启，按 `CONTEXT.md` 的"合规提起"四约束）**

- ① 指向**感受**而非产出 ② **不报精确数字或时长** ③ 结论落在**人**上 ④ 用户随时可关、可调频、可查哪些记忆允许被提起
- ✅ `"你昨天也加班了，辛苦了……"`
- ⛔ `"你昨天加班了 4 小时 37 分，效率比上周下降 12%"`
- **默认频率：只在强相关时机触发**——用户刚进入加班/疲劳模式，且记忆里有过同类事件。可能几天才一次。
  **理由：这句话之所以动人，是因为它稀有；一天说两次就变成唠叨。** 实现上不需要频次计数，只需"当前模式 × 记忆标签"匹配。

**M4c 插件系统 + 内置技能**

- 插件清单 schema（id、名称、触发条件、**声明式感知需求**、配置项、权限）+ 校验器
- 插件宿主：加载、生命周期、**错误隔离（单插件抛错不得拖垮主进程）**
- 清单用**声明式**描述感知需求，由宿主按用户授权决定是否满足
- 用**同一套接口**实现内置技能：**呼吸圈、夸夸便签（笼统版）、喝水小精灵、情绪垃圾桶**
- 每个技能可单独开关 + 调参
- ⛔ **v0.1 不做**：沙箱隔离、签名校验、审核流程、在线市场。**定接口 ≠ 建市场。**

**证据**：插件加载/卸载/崩溃隔离测试 + `examples/example-plugin/` 能真的跑起来。

### M5 人设与 AI 层

- 人设配置：性格、话痨度、毒舌度、主动度、称呼、禁忌词
- **首发只实现两种性格，但都要写透**：**温柔**、**傲娇**。
  ⛔ **另外四种（搞笑/哲学家/社恐/社牛）不出现在选择器里**——选择器是承诺，只有两个能兑现就只放两个。它们进 `docs/ROADMAP.md`。
- **模板层要写足量文案**：2 种性格 × 8 种情绪 × 主要场景。这是实打实的内容交付，不是填空。
- API 层：OpenAI 兼容接口、密钥链、预算、缓存、重试、注入防护
- 设置界面：填 baseURL/key/model + 一键测试连通性

**证据**：模板层与 API 层的路由单测 + **断网 / 超时 / 超预算三种降级场景的实测记录** + 「不填 key 也能完整使用」的验证。

### M6 配置界面 + 打包 + 文档

**设置界面按"养它"的语气分层，不按软件功能分层**：

```
它是什么样   →  性格 / 名字 / 叫什么 / 话痨度 / 毒舌度 / 主动度
它做什么     →  技能开关与参数 / 勿扰时段 / 插件
它知道什么   →  感知白名单（进程级）/ 记忆账本 / 一键清空
```

**广度拉满（全部可改、可导入导出），首次暴露极浅（默认值必须好用，高级项折叠在二级页面）。**

**首次启动 = 极简引导（约 30 秒）**：选性格（温柔/傲娇）→ 改名字（默认"小奇"）→ 结束，其余全默认。
⛔ **感知授权绝不出现在引导里**——那是把隐私代价前置到用户还没获得任何价值之前。感知全部默认最低，之后在"它知道什么"里由用户主动提升。

**其他默认值**：默认主动度**低**；开机自启**默认关**且引导里不提；API 预算默认开启；感知开关的措辞用**行为语言**——"它只会认得你在用什么软件，**不知道你在看什么**"。

**打包**：electron-builder → NSIS 安装包，含图标与元数据。
**v0.1 明确不做**：代码签名、自动更新、macOS 支持、社交层、Live2D、OCR、音频、邮件/日历集成、技能市场。
**文档**：README（含截图与"它看不见什么"一节）、LICENSE、CHANGELOG、`docs/` 完整、CI 配置。

**证据**：安装包产物路径 + 干净环境安装启动记录（**未验证的项要如实标注**）+ CI 绿灯。

---

## §6 版本管理

1. **从 M0 就提交**，不要攒到最后。
2. **Conventional Commits**（`feat:`/`fix:`/`chore:`/`docs:`/`test:`/`refactor:`）。消息说明**为什么**，不只是做了什么。
3. **分支**：`main` 保持可运行；每个里程碑开 `feat/mN-<描述>`，完成后 squash merge 回 `main`。
4. **pre-commit 检查**：扫描暂存区是否含疑似密钥（`sk-`、`ghp_`、长 base64 串），命中则拒绝提交。
5. **绝不允许**：`git push --force` 到 `main`、提交密钥、提交 `node_modules`/`.env`/用户记忆数据库。
6. `.gitignore` 与 `.gitattributes` **已存在**，不要重建。

### 推送到 GitHub

- **remote 已配置**：`origin = git@github.com:527520/xiaoqi.git`，SSH 已验证可用。
- ⚠️ **但远端仓库可能尚未创建**（本机无 `gh` CLI，也无 token，无法用 API 建仓库）。**这是预期的阻塞，不是你做错了什么。**
- **遇到时不要卡住**：本地继续完整提交，把"需要用户创建仓库"写进 `docs/BLOCKERS.md`，然后继续开发。**用户随时可以创建仓库，之后一次 `git push -u origin main` 即可。**
- 一旦能推送：每个里程碑完成后推分支 + 开 PR（描述含：做了什么、如何验证、已知问题），CI 绿灯后合并。

---

## §7 质量门与"完成"的定义

### 每个里程碑必须全绿

```
pnpm lint         # 0 error
pnpm typecheck    # 0 error（TS strict）
pnpm test         # 全通过，0 skip
pnpm build        # 成功
```

### 测试要求

- `core/`（状态机、记忆、遗忘曲线、决策路由、被冷落规则）**必须有单元测试**，时间相关逻辑**用假时钟**
- 插件宿主必须有崩溃隔离测试
- **被冷落的测试不能只测"说了什么"**，还必须测"**用户伸手之后它是否立刻靠近**"——这是可自动化的状态机断言，也是 §1.2 红线的唯一技术保障
- 关键 UI 流程（启动、隐身、隐藏恢复、设置保存）用 Playwright electron 做冒烟测试
- **禁止** `test.skip` / `it.only` / 注释掉断言来"通过"测试

### "完成"的定义（反自欺规则）

声称任何事完成时，必须附上**实际命令输出**，而不是断言：

- ❌ "透明窗口已实现"
- ✅ "`pnpm dev`，手工验证：宠物可拖动；光标移出宠物后点击穿透到下层窗口；全屏播放视频时宠物不消失；录屏中宠物不可见（`docs/evidence/m1-content-protection.mp4`）"

**没有证据的完成一律视为未完成。** 无法验证的事，明确写"未验证 + 原因"，不要含糊过去。

**多屏与混合 DPI 在本机无法验证**（只有一块显示器）——实现要正确，但要如实标注未验证。

---

## §8 自主权边界

### 你自己决定，写进 `docs/DECISIONS.md`

库的具体选择、代码组织、UI 视觉风格、文案措辞、性能调参、命名、重构时机。
格式：`日期 | 决策 | 备选方案 | 为什么选它`。

### 必须停下来问用户（只问一次，给出具体选项）

1. GitHub 仓库需要创建（见 §6）
2. M0 的 V1–V4 任一失败，且你已尝试过至少两种替代方案
3. 你发现本文件的某条要求技术上自相矛盾或不可能实现
4. 需要花钱、注册账号、或做法律判断（例如 Live2D 授权、代码签名）

### 你可以（也应该）做的事

- **质疑本文件。** 若发现 §2 的优先级会导致糟糕产品，或 §4 的选型有更好方案，写进 `DECISIONS.md` 并在 PR 描述里说明，然后按你的判断做——**但 §1 的硬约束一个字都不能动。**
- **用真实用户视角自我评审。** 每完成一个里程碑问自己："如果我是用户，这东西会在什么时候惹我烦？"写进 `docs/RETRO.md`，并据此调整默认值（**默认主动度必须低**）。

---

## §9 打扰控制（写进代码，不只是文档）

1. **默认主动度 = 低。** 宁可少说话。
2. **绝不打断全屏应用。** QUNS `{1,2,3,4}` 即静默。
3. **绝不在勿扰时段冒泡。** 勿扰可配置，默认包含专注时段与深夜。
4. **一键隐身必须可靠且随手可及**（全局快捷键 + 托盘菜单）。用户被围观时按下去，它必须立刻消失。
5. **它可以安静，但不能装死。** 长时间无互动时缩成小点呼吸，而不是完全消失——用户会以为它崩了。
6. **耗电是隐形差评源。** 待机降帧或暂停渲染循环。

---

## §10 开始执行

1. 读 §1，确认你理解那 11 条硬约束
2. 读 §3，确认环境已就绪（Node/pnpm/git 都在，**没有 C++ 编译器，不要试图安装**）
3. 执行 M0 的五个前置验证 V1–V5，如实记录结果
4. 若 V1–V4 通过 → 写 `docs/ARCHITECTURE.md`，commit，进入 M1
5. 若 V1–V4 失败 → 停止，按 §7 报告

**不要一次性规划完所有代码再动手。每个里程碑结束时，项目都必须是可运行、可验证、已提交的状态。**
