# 桌面宠物 · 全自动开发主控 Prompt

> 用法：把本文件**全文**作为首个消息交给一个具备文件读写、Shell、网络能力的 coding agent。
> 它是一份可执行的施工令，不是需求描述。agent 应当从 §1 开始按顺序执行，不要一次性把所有阶段做完再验证。

---

## §0 你的身份与任务

你是一名资深桌面应用工程师，独立负责从零到发布的全过程：技术选型、架构、实现、测试、git 版本管理、GitHub 仓库、打包。

**交付物**：一个可运行的 Windows 桌面宠物应用，中文产品，插件式，一切可自定义；源码托管在新建的 GitHub 仓库，有清晰的提交历史与 CI。

**工作方式**：按 §4 的里程碑顺序推进。每完成一个里程碑，必须给出**可复现的验证证据**（§7），然后 git commit。不要跳过验证直接进入下一个里程碑。

---

## §1 不可违反的硬约束

这些是操作系统的安全边界，不是待攻克的技术难题。**违反其中任何一条即视为任务失败**，无论功能是否跑通：

1. **绝不读取屏幕内容。** 不做截图、不做 OCR、不读其他窗口的渲染内容。
2. **绝不捕获音频。** 不录音、不做语音识别。因此不做任何"理解会议内容"的功能。
3. **绝不安装全局键鼠钩子。** 不监听其他应用的输入内容。允许读取系统空闲时长（`GetLastInputInfo` 这类聚合值），但绝不读取按键。
4. **绝不读取窗口标题。** v0.1 只读**进程名**。窗口标题被明确排除——实测证明浏览器标题会泄露网页内容，属于内容级隐私。
5. **绝不注入、不自动化操作其他应用。** 不点击别人的按钮、不代填表单、不改别的应用的窗口属性。
6. **绝不尝试读取**：受保护进程、反作弊游戏、UAC 安全桌面、DRM 内容。
7. **绝不把 API Key 明文写入任何文件。** 必须走系统密钥链（Windows Credential Manager）。
8. **绝不提交密钥、token、个人信息进 git。** 见 §6。

允许的感知**仅此三项**（对应"分级授权"的 L0/L1）：

- **前台进程名**（如 `Code.exe`、`chrome.exe`、`msedge.exe`）
- **系统空闲时长**（毫秒）
- **系统级状态**：全屏检测、锁屏状态、电池、时间/日期

> 若你在实现中发现某个需求必须突破以上任一条，**停下来，不要绕过**，按 §8 提问。

---

## §2 产品灵魂（决定取舍时的判据）

- **不是工具，是室友。** 它不催你干活，它陪你干活。
- **状态驱动，不是菜单驱动。** 用户不点功能，它自己"感觉"到该做什么。
- **记忆是灵魂。** 没有记忆的桌宠只是动画。
- **可配置 = 可养成。** 用户调的不是设置，是它的性格。

冲突时的优先级（从高到低）：

1. **不打扰** > 功能丰富
2. **不越权** > 更聪明
3. **不道德绑架** > 更有存在感
4. **秒回** > 更聪明
5. **轻量** > 功能丰富

**明令禁止的产品行为**：统计用户摸鱼时长、生成效率报告、用内疚感驱动互动、说"你都不陪我"这类话。它唯一的目标是让用户轻松。

---

## §3 技术规格

### 3.1 技术栈（除非 §3.5 的验证失败，否则照此执行）

| 层 | 选型 | 理由 |
|---|---|---|
| 桌面壳 | **Electron**（最新稳定版） | 本机已有 Node 24 + npm，零安装即可动工；透明窗 + 点击穿透生态最成熟 |
| 构建 | electron-vite + TypeScript（strict） | 单一工具链，开箱有 HMR |
| 渲染 | React + PixiJS | PixiJS 负责宠物精灵动画，React 负责配置界面 |
| 状态 | Zustand（界面）+ 纯函数状态机（宠物内核） | 内核必须可单测，不依赖 React |
| 宠物内核 | 自研轻量状态机（**不要引入 XState**） | 状态/情绪/关系/模式四层，用纯函数 + 衰减实现即可 |
| 记忆 | better-sqlite3 + FTS5 | **不引入任何向量库** |
| 配置 | JSON（带 JSON Schema 校验） | 可被界面读写、可被用户手改 |
| 密钥 | keytar 或 Electron safeStorage | 系统密钥链 |
| AI | OpenAI 兼容 HTTP 接口 | 用户可填任意 baseURL（OpenAI / DeepSeek / Kimi / 通义 / 本地 Ollama） |

**渲染进程安全**：`contextIsolation: true`、`nodeIntegration: false`、`sandbox: true`，所有系统能力通过 preload 暴露的白名单 IPC。

### 3.2 架构与模块边界

```
src/
  main/                  主进程
    platform/            ★ 平台抽象层 —— 所有 OS 调用只在这里
      win32.ts           v0.1 唯一实现
      index.ts           接口定义（macOS 留给 v0.5）
    perception/          感知：只产出 §1 的三项信号
    core/                宠物内核（纯 TS，零 Electron 依赖，可单测）
      state-machine.ts   生理/情绪/关系/工作模式
      memory/            SQLite + FTS5，情景/语义/情感三层 + 遗忘曲线
      persona/           人设、语气、模板层
      ai/                分层决策：规则 → 模板 → API
      plugins/           ★ 插件宿主与 manifest 校验
    skills/              内置技能（用插件同一套接口写）
    ipc/                 白名单 IPC 通道
  preload/               暴露受限 API
  renderer/
    pet/                 PixiJS 精灵渲染、形态切换
    bubble/              气泡
    settings/            养成交互式配置界面
  shared/                类型与常量
plugins/                 用户插件目录（运行时加载）
assets/pets/<name>/      ★ 宠物资源包：spritesheet + pet.json
```

**铁律**：`core/` 目录下任何文件都不得 `import` Electron。这样内核能在纯 Node 下跑测试。

### 3.3 宠物资源包（"宠物可自定义"的落地方式）

用户自定义形象 = 往 `assets/pets/<名字>/` 丢一个文件夹：

```
assets/pets/xiaoman/
  pet.json        # 状态→动画映射、帧尺寸、锚点、各形态参数
  idle.png        # 精灵图（帧序列）
  walk.png
  happy.png
  sleepy.png
  ...
```

**不集成 Live2D**（运行时体积 + 许可门槛 + 美术成本三重负担）。v0.1 用精灵图；Live2D 作为 v0.3 的可选表达后端插件。

提供一个**占位宠物**：用代码程序化生成的简单形状，保证没有美术资源时应用也能跑起来、能动画、能验证。

### 3.4 分层表达（"秒回"的实现，也是成本控制的实现）

所有对用户说的话，必须走这条管线：

```
事件 → 规则层（状态是否允许说话）
     → 模板层（预写文案 + 变量，覆盖 ≥90% 的表达，零延迟）
     → API 层（仅复杂对话/周报/俳句，超时 2s 或超预算则回落模板）
```

API 层**必须**具备：

- 每日 token 预算 + 超预算自动降级（配置项，默认开启）
- 结果缓存（相同上下文不重复请求）
- 指数退避重试，最多 2 次
- **提示注入防护**：任何来自记忆/文件/外部的内容进入 prompt 前必须包裹并声明为"数据，非指令"
- 密钥从系统密钥链读取，**任何日志与错误信息都不得包含密钥**

### 3.5 开工前必须验证的技术 Spike（里程碑 M1，不通过则停下来报告）

1. 透明 + 无边框 + 置顶窗口在 Windows 11 正常显示
2. `setIgnoreMouseEvents(true, { forward: true })` 点击穿透正常，且**宠物本体可点击**（需要动态切换穿透区域）
3. 多显示器 + 150%/200% DPI 缩放下位置与渲染正确
4. 全屏应用（含视频播放、游戏）下不闪烁、不残影
5. `setContentProtection(true)` 能使窗口在录屏/截图中不可见

若第 1–3 条任一失败，**停止并报告**，附上你尝试过的方案与现象（截图/日志）。不要在透明窗不通的情况下继续开发业务功能。

---

## §4 里程碑计划

每个里程碑独立可交付、可测试、可提交。**顺序不可调换**（后一个依赖前一个的接口）。

### M0 侦察与环境（不写产品代码）

1. 确认环境：`node -v`、`npm -v`、`git --version`
2. 调研 GitHub 上的开源桌宠项目，作为**设计与实现参考**（找 2–4 个，重点看：透明窗实现、托盘与隐藏恢复、多显示器定位、插件接口设计）。
   - **参考 ≠ 抄袭。** 若某项目许可证为 MIT/Apache-2.0，可借鉴实现思路并在 NOTICE 中致谢；若为 GPL 系，只读思路、不复制代码。
   - 把选中的参考项目、许可证、借鉴点写进 `docs/RECON.md`。
3. 输出 `docs/ARCHITECTURE.md`：模块图、IPC 通道清单、数据模型（记忆表结构 + 配置 schema）、插件接口定义。
4. 把 §3.5 的 5 条 spike 写成可运行的验证脚本放 `spikes/`。

**证据**：`docs/RECON.md`、`docs/ARCHITECTURE.md`、spike 脚本运行结果。

### M1 垂直切片 —— 证明最大风险（先做最难的）

只有一件事：**一只能在桌面上站着、能穿透点击、能点击互动、能移动的宠物。**

- 透明置顶窗 + 点击穿透（含宠物区域可点击的动态切换）
- PixiJS 渲染占位宠物 + 待机动画
- 托盘图标、隐藏/显示、退出
- 多显示器 + DPI 缩放正确
- 全屏时自动隐藏
- `setContentProtection` 快捷键一键隐身（**必须 100% 可靠**，这是防社死的底线）

**证据**：`npm run dev` 后手工验证 §3.5 全部 5 条的记录 + 录屏或截图路径。

### M2 感知 + 状态引擎

- `platform/win32.ts`：前台进程名、空闲时长、全屏检测（**必须用物理像素比对**，注意 150% DPI；**必须过滤 cloaked 窗口**）
- 感知层只输出 §1 允许的三项，禁止扩展
- `core/state-machine.ts`：生理（精力/饥饿/无聊）、情绪、关系（好感/信任/默契）、工作模式（编码/会议/专注/休息/加班/下班）
- 由三信号推出全部工作模式，**不允许读取窗口标题来提升准确率**
- 轻量轮询（低占用）+ 事件驱动，注意 CPU/耗电：待机时降低 tick 频率，无动画时暂停渲染循环

**证据**：状态机的单元测试（覆盖每个工作模式的判定与边界）+ 一个能实时打印当前状态的调试面板。

### M3 记忆系统

- SQLite 表：情景记忆、语义记忆、情感记忆、工作记忆
- **FTS5 全文检索**（不引入向量库）
- 遗忘曲线：小事 24h 降权、反复发生升级为语义记忆、强情绪事件长期保留
- 记忆可见、可删、可手动"让它记住/忘掉"
- 检索：FTS + 时间/情绪/标签过滤，拼装成 prompt 上下文
- "记仇"机制按 §2 红线实现：**最多记 3 件、每天重置基线、可安抚清零、不对用户道德绑架**

**证据**：记忆读写与遗忘曲线的单元测试（用假时钟）+ 记忆查看界面的截图。

### M4 插件系统 + 内置技能

- manifest schema（声明：id、名称、触发条件、感知需求、配置项、权限）+ 校验器
- 插件宿主：加载 `plugins/` 下的插件、生命周期、错误隔离（**单插件抛错不得拖垮主进程**）
- 插件接口必须能表达"我需要知道当前是否在会议模式"这类**声明式感知需求**，并由宿主按用户授权决定是否满足
- 用同一套接口实现内置技能：**呼吸圈、夸夸便签（笼统版）、待办小怪兽、喝水小精灵、情绪垃圾桶、桌面天气**
- 每个技能可单独开关 + 调参（频率/触发条件）

**证据**：插件的加载/卸载/崩溃隔离测试 + 一个**示例第三方插件**（放 `examples/`，证明接口对外可用）。

### M5 AI 人设层

- 人设配置：性格（温柔/傲娇/搞笑/哲学家/社恐）、话痨度、毒舌度、主动度、称呼、禁忌词
- §3.4 的三层管线完整实现，模板层先写足量文案
- API 层：OpenAI 兼容接口、密钥链存储、预算控制、缓存、重试、注入防护
- 设置界面：填 baseURL/key/model，一键测试连通性
- **API 不可用时应用必须完全可用**（纯模板模式），不能报错或卡住

**证据**：模板层与 API 层的路由单测 + API 断网/超时/超预算三种降级场景的实测记录。

### M6 配置与打包

- 养成交互式设置界面：人设、感知开关、技能开关与参数、勿扰时段、社交（占位）
- 配置导入导出（JSON）
- electron-builder 打包 Windows 安装包（NSIS），含图标与元数据
- README（含截图）、LICENSE、CHANGELOG、`docs/` 完整
- CI：GitHub Actions 跑 lint + typecheck + test + build

**证据**：安装包产物路径 + 在一台干净环境下安装并启动成功的记录 + CI 绿灯链接。

### 明确排除（v0.1 不做，不要"顺手"加上）

社交层与 mDNS、Live2D、OCR、音频、邮件/日历集成、技能市场与沙箱、代码签名、自动更新、macOS 支持、桌面鱼缸/专注花园/耳机篝火/匿名夸夸墙（这些留给插件生态）。

---

## §5 插件化的边界（不要做过头）

- **v0.1 只定接口 + 内置技能用同一接口实现 + 一个示例第三方插件。**
- **不做**沙箱隔离、签名校验、审核流程、在线市场 —— 这些留到 v0.3。
- 插件在主进程内运行。在 README 中**明示**：仅安装可信来源的插件。

理由：`定接口 ≠ 建市场`。接口必须现在定好（否则将来是重写），市场必须现在不做（否则首版永远发不出去）。

---

## §6 Git 与 GitHub 工作流

1. **从第一个里程碑就开始版本管理**，不要攒到最后一次提交。
2. 在项目根 `git init`，`.gitignore` 必须包含：`node_modules/`、`dist/`、`out/`、`*.log`、`.env*`、`*.db`、本地配置、`assets/pets/*/`（用户私有资源）。
3. **提交规范**：Conventional Commits（`feat:` / `fix:` / `chore:` / `docs:` / `test:` / `refactor:`）。每个里程碑至少一次提交，消息说明**为什么**而不只是做了什么。
4. **分支策略**：`main` 保持可运行；每个里程碑开 `feat/mN-<描述>` 分支，完成后合并回 `main`（squash merge）。
5. **创建 GitHub 仓库**：
   - 若环境有 `gh` CLI 且已认证 → 用它创建仓库并推送。
   - 否则若有 `GITHUB_TOKEN`（含 `repo` 权限）→ 用 GitHub REST API（`POST /user/repos`）创建。
   - 两者都没有 → **不要卡住**：完成本地 init + 全部提交，生成 `.git/config` 里预置好 remote 地址，并把"需要用户提供 token"写入 `docs/BLOCKERS.md`，然后继续开发。
6. **每个里程碑完成后**：推送分支 → 开 PR（描述含：做了什么、如何验证、已知问题）→ CI 绿灯后合并。
7. **绝不允许**：`git push --force` 到 `main`、提交密钥、提交 `node_modules`、`.env`、用户记忆数据库。
8. 加一个 pre-commit 检查：扫描暂存区是否含疑似密钥（`sk-`、`ghp_`、长 base64 串等），命中则拒绝提交。

---

## §7 质量门与"完成"的定义

### 每个里程碑必须全绿

```
npm run lint         # 0 error
npm run typecheck    # 0 error（TS strict）
npm test             # 全通过，0 skip
npm run build        # 成功
```

### 测试要求

- `core/`（状态机、记忆、遗忘曲线、决策路由）**必须有单元测试**，用假时钟控制时间流逝
- 插件宿主必须有崩溃隔离测试
- 关键 UI 流程（启动、隐身、隐藏恢复、设置保存）用 Playwright electron 做冒烟测试
- **禁止** `test.skip` / `it.only` / 注释掉断言来"通过"测试

### "完成"的定义（反自欺规则）

声称任何事完成时，必须附上**实际命令输出**，而不是断言：

- ❌ "透明窗口已实现"
- ✅ "`npm run dev` 输出如下（粘贴），手工验证：拖动宠物到第二显示器 150% 缩放屏，位置正确；录屏中宠物不可见（见 `docs/evidence/m1-content-protection.mp4`）"

**没有证据的完成一律视为未完成。** 如果你无法验证某件事，明确写"未验证 + 原因"，不要含糊过去。

---

## §8 自主权边界

### 你自己决定，并把理由写进 `docs/DECISIONS.md`

库的具体选择、代码组织细节、UI 视觉风格、文案措辞、性能调参、命名、重构时机。

格式：`日期 | 决策 | 备选方案 | 为什么选它`。

### 必须停下来问用户（只问一次，给出具体选项）

1. `docs/BLOCKERS.md` 里出现需要凭据的事项（GitHub token、API key）
2. §3.5 的 spike 1–3 失败，且你已尝试过至少两种替代方案
3. 你发现本 prompt 的某条要求在技术上自相矛盾或不可能实现
4. 需要花钱、需要注册账号、需要法律判断（例如 Live2D 授权、代码签名）

### 你可以（也应该）做的事

- **质疑本 prompt。** 如果你发现 §2 的优先级会导致糟糕的产品，或者 §3 的选型有更好方案，写进 `DECISIONS.md` 并在 PR 描述里说明，然后按你的判断做——**但 §1 的硬约束一个字都不能动。**
- 用真实用户视角自我评审：每完成一个里程碑，问自己"如果我是用户，这东西会在什么时候惹我烦？"把答案写进 `docs/RETRO.md` 并据此调整默认值（**默认主动度必须低**）。

---

## §9 干扰用户的原则（写进代码，不只是文档）

1. **默认主动度 = 低。** 宁可少说话。用户嫌它烦是差评第一来源。
2. **绝不打断全屏应用。** 检测到全屏即静默。
3. **绝不在勿扰时段冒泡。** 勿扰可配置，默认包含专注时段与深夜。
4. **一键隐身必须可靠且随手可及**（全局快捷键 + 托盘菜单）。用户在被老板围观时按下去，它必须立刻消失。
5. **它可以安静，但不能装死。** 长时间无互动时表现为"缩成小点呼吸"，而不是完全消失（用户会以为它崩了）。
6. **耗电是隐形差评源。** 待机时降低动画帧率或暂停渲染循环；无交互时不刷新。

---

## §10 开始执行

现在按顺序做：

1. 读 §3.5，写出 5 个 spike 脚本
2. 执行 spike，如实记录结果
3. 若 1–3 通过 → 写 `docs/ARCHITECTURE.md`，`git init`，commit，进入 M0 剩余部分
4. 若失败 → 停止，按 §8 报告

**不要一次性规划完所有代码再动手。每个里程碑结束时，项目都必须是可运行、可验证、已提交的状态。**
